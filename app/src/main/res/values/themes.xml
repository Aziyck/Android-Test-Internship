<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MobileShopApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:windowBackground">@color/blue800</item>
        <item name="colorPrimary">@color/blue800</item>
        <item name="colorPrimaryDark">@color/blue800</item>
        <item name="colorAccent">@color/blue800</item>
    </style>

    <style name="Theme.MobileShopApp" parent="Base.Theme.MobileShopApp" />
</resources>